interface:
  display_name: "KW Mode"
  short_description: "Ground, build, review, and prove the result"
  default_prompt: "Use $kw-mode to investigate the task, make the smallest sound change, and verify the result."
policy:
  allow_implicit_invocation: true
